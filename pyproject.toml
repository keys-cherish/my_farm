[project]
name = "my-farm"
version = "0.1.0"
description = "Telegram Farm Game Bot"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "python-dotenv>=1.2.2",
    "python-telegram-bot[ext]>=22.6",
    "uvloop>=0.21.0; sys_platform != 'win32'",
]
